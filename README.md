# Dependencies
## FrontEnd Dependencies
## BackEnd Dependencies
### Initialize (Sekali aja)
npm init -y

### Selalu pastikan dalam directory server 
cd server

### Install
- npm install express firebase-admin mqtt dotenv cors
- npm install --save-dev nodemon 